一. 继承的目的:
    1. 简洁代码, 通过将多个类中共有的属性或方法提取出来做成父类成员,然后子类直接继承父类成员.
    2. 多态, 即父类中成员不具体实现,而是子类重写该方法时自己各自实现.

二. 语法
    1. class a extends b{}

三. 特点
    1. 子类继承父类的成员. 这是单向的,父类无法使用子类的成员.
    2. java是单继承、多重继承. 即 父-子 只能是1对1 或 1对N
    3. 父类的私有成员子类无法直接使用

    4. 使用时: Fu f = new Zi();
        在使用属性时, 用的是左边,即Fu
        在使用方法时, 用的是右边,即Zi


四. 继承中的关键字和方法重写

    1. super关键字
        用途: 在子类中访问父类的成员, 在子类构造器中访问父类构造器
        使用方法: super.成员 或 在子类构造器中首行 super();
        构造器执行逻辑: 每当创建子类对象时, 会先执行父类的无参构造,再当前构造; 没有显式写则虚拟机默认补全.
    2. this关键字: 代表当前对象.


    3. 继承中的方法重写:
            使用常见: 父类中方法不满足当前需求,但不能随意改动. 因此在子类中实现.
            5.1 子类不能重写父类的构造器和私有方法和Static方法.
            5.2 重写权限: 子类访问权限>=父类.  子类返回值和抛出异常 <= 父类
